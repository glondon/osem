$(".alert").remove();
$('#users_area').html("<%= escape_javascript(render partial: 'users', locals: { users: @users || @role.users }).html_safe %>");

if ( $.fn.dataTable.isDataTable( '#users' ) ) {
      table = $('#users').DataTable();
    }
else {
  $('#users').DataTable({
    autoWidth: false,
    pagingType: 'full_numbers',
    "lengthMenu": [[25, 50, 100, -1], [25, 50, 100, "All"]]
  });
}
